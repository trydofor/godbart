package internal

import "fmt"

func Move(pref *Preference, dest []DataSource, source *DataSource, tree *TreeMoving, test bool) (err error) {

	fmt.Println(dest)
	fmt.Println(source)
	fmt.Println(test)
	return nil
}
