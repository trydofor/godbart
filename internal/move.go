package internal

import "fmt"

func Move(pref *Preference, dest []DataSource, source *DataSource, sqls []FileEntity, test bool) (err error) {

	fmt.Println(dest)
	fmt.Println(source)
	fmt.Println(test)
	return nil
}
